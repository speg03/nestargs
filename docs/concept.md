# Concept

`nestargs` was created to simplify working with structured data through command-line arguments.

It is useful when a group of values should be treated as a single unit while allowing specific parts of the data to be modified. By expressing relationships between values within argument names, `nestargs` preserves that structure after parsing, making each value accessible through a hierarchy.

For detailed usage, see the [User guide](guide.md).
